#!/usr/bin/env ruby

require './lib/ice_cream_final' #keep at top to grab BlueBellFinal module - anything that needs my environment can run this one line.

IceCream::CLI.new.call #everything is programmed into this object - creates a new instance of CLI class and starts the call method on it.